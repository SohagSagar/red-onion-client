import React from 'react';
import { useQuery } from 'react-query';

const GetAllUser = () => {
    // const { data: products, isLoading, refetch } = useQuery(['products'], () => fetch(`http://localhost:5000/all-user`).then (res=>res.json()));
    // if(isLoading){
    //     return <>Loading</>
    // }
    
    return (
        <div>
            get all users:
        </div>
    );
};

export default GetAllUser;